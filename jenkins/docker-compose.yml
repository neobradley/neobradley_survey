version: '3'
services:
  jenkins:
    image: jenkins/jenkins:lts
    container_name: jenkins
    restart: always
    ports:
      - '9080:8080'
      - '59527:50000'
    volumes:
      - ./var/jenkins_home:/var/jenkins_home